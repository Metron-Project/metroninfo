[project]
name = "metroninfo"
version = "0.1.0"
description = ""
authors = [{ name = "Brian Pepple", email = "brian@metron.cloud" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "xmlschema>=3.4.2",
    "pytest>=8.3.3",
    "ruff>=0.7.1",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = "tests/test*"
